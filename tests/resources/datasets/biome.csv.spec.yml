# data path (could be also a list)
path: resources/data/dataset_source.csv

# data format (infered from path extension if no provided)
format: csv

# mapping model-dataset definition
forward:
  tokens:
  - age
  gold_label: job

# Extra arguments passed to pandas.read_csv method
encoding: UTF-8
delimiter: ","
