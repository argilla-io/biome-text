datasource:
  id: training datasource
  name: Training data source
  params:
    path:
    - first_train.csv
  format:
    type: csv
    lang: de
    charset: UTF-8
    params:
      delimiter: ","

settings:
  modelConnect:
    tokens:
    - name
    target:
      gold_label: category
